import type { Theme } from "src/theming/types";
import fonts from "./fonts";
import colors from "./colors";

const MaterialYou: Theme = {
  version: "m3",
  colors,
  fonts,
  typography,
};

export default MaterialYou;
