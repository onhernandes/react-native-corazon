import type { Colors, Fonts, Theme } from "../types";

export const colors: Colors = {
  primary: "",
  secondary: "",
  background: "",
};

export const fonts: Fonts = {};

const MaterialYou: Theme = {
  version: "m3",
  colors,
  fonts,
};

export default MaterialYou;
